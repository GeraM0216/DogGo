<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] — DogGo</title>

    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="@Url.Content("~/css/site.css")" />

    @await RenderSectionAsync("Styles", required: false)
</head>
<body>

    <!-- ── Navbar ── -->
    <nav class="navbar navbar-expand-md navbar-doggo sticky-top">
        <div class="container-fluid px-3">

            <a class="navbar-brand" asp-controller="Home" asp-action="Index">
                <img src="@Url.Content("https://cdn.phototourl.com/free/2026-04-07-4b0ab729-6be0-43c9-96bb-2189d1589f86.png")" alt="DogGo" class="nav-logo-img" />
            </a>

            <button class="navbar-toggler border-0" type="button"
                    data-bs-toggle="collapse" data-bs-target="#navMenu">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navMenu">
                <ul class="navbar-nav me-auto gap-1 ms-2">
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Home" asp-action="Index">Inicio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Paseador" asp-action="Directorio">Paseadores</a>
                    </li>

                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Paseo" asp-action="MisPaseos">Mis paseos</a>
                        </li>

                        @if (User.IsInRole("Paseador"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Paseador" asp-action="Perfil">Mi perfil</a>
                            </li>
                        }

                        @if (User.IsInRole("Duenio"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Perro" asp-action="Index">Mis perros</a>
                            </li>
                        }
                    }
                </ul>

                <ul class="navbar-nav gap-1 align-items-center">
                    @if (User.Identity.IsAuthenticated)
                    {
                        var nombre = User.FindFirst(System.Security.Claims.ClaimTypes.Name)?.Value;
                        var rol = User.FindFirst(System.Security.Claims.ClaimTypes.Role)?.Value;
                        var rolMostrado = rol == "Duenio" ? "Cliente" :
                                          rol == "Paseador" ? "Paseador" : rol;

                        <li class="nav-item d-flex align-items-center gap-2 px-2">
                            <span class="nav-user-name">👤 @nombre</span>
                            <span class="nav-badge-rol">@rolMostrado</span>
                        </li>
                        <li class="nav-item">
                            <form asp-controller="Auth" asp-action="Logout" method="post" class="m-0">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn-logout">Cerrar sesión</button>
                            </form>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Auth" asp-action="Login">Iniciar sesión</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-cta-btn" asp-controller="Auth" asp-action="Register">Registrarse</a>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </nav>

    <!-- ── TempData Alerts ── -->
    @if (TempData["Exito"] != null)
    {
        <div class="alert alert-success alert-dismissible tempdata-alert" role="alert">
            ✅ @TempData["Exito"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible tempdata-alert" role="alert">
            ❌ @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <main>
        @RenderBody()
    </main>

    <!-- ── Footer ── -->
    <footer class="footer-doggo">
        <div class="footer-inner">
            <img src="@Url.Content("https://cdn.phototourl.com/free/2026-04-07-4b0ab729-6be0-43c9-96bb-2189d1589f86.png")" alt="DogGo" class="footer-logo-img" />
            <span>DogGo &copy; @DateTime.Now.Year — Proyecto universitario</span>
        </div>
    </footer>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/js/bootstrap.bundle.min.js"></script>
    <script>
        document.querySelectorAll(".tempdata-alert").forEach(a => {
        setTimeout(() => bootstrap.Alert.getOrCreateInstance(a).close(), 4000);
        });
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>